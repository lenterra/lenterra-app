node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# macOS
.DS_Store

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

.env
.env.local
biome.json
.yalc/
yalc.lock
android/
ios/
eas/
credentials.json
public/.well-known/apple-app-site-association
public/.well-known/assetlinks.json
eas-android.md
# Jest writes this on every `test:ci` run. A generated report in the history
# turns a one-line source change into a thousand-line diff, and the numbers in
# it are stale the moment they are committed — the gate in CI is the record.
coverage/
